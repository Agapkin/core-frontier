# Stage 02.4.4 — Responsive Mobile UX / Adaptive HUD

## Цель этапа

Адаптировать интерфейс CORE FRONTIER под реальные мобильные устройства и планшеты.

Stage 02.4.3 успешно разделил код на модули, но тестирование на телефоне и iPad показало, что текущий UI плохо масштабируется:

- HUD занимает слишком много места;
- кнопки перекрывают игровое поле;
- portrait и landscape используют один и тот же layout;
- zoom / speed / bottom controls создают визуальный шум;
- wave panel перекрывает карту;
- интерфейс не учитывает safe-area браузера и мобильных устройств.

Главная цель Stage 02.4.4:

сделать игру читаемой и управляемой на телефоне, планшете и в landscape-режиме без изменения core gameplay.

---

# Главный принцип этапа

На этом этапе НЕ добавляем новые gameplay-механики.

Stage 02.4.4 решает именно UX / UI / responsive layout.

Новые игровые системы вроде генераторов, salvage, харвестеров, технологий и расширения карты фиксируются как будущий backlog, но не реализуются в этом этапе.

---

# Проблемы, выявленные тестированием

## 1. HUD перегружен

Сейчас верхняя панель содержит:

- дерево;
- камень;
- еду;
- HP базы;
- волну;
- энергию;
- сложность;
- zoom.

На мобильном экране это превращается в несколько рядов крупных блоков и закрывает карту.

---

## 2. Wave panel занимает слишком много места

Текущая панель волны отображается прямо на canvas и занимает большую область карты.

На телефоне она мешает gameplay.

---

## 3. Zoom и speed controls создают лишний шум

Сейчас отдельно отображаются:

- zoom controls;
- speed controls;
- zoom percent в HUD;
- zoom percent в нижней панели.

Это создаёт дублирование.

---

## 4. Bottom buttons слишком крупные

На телефоне кнопки:

- Башня;
- Волна;
- Codex;
- Меню;

занимают слишком много ширины и высоты.

---

## 5. Portrait и landscape требуют разных layout

В portrait нужно освобождать вертикальное пространство.

В landscape нужно освобождать горизонтальное пространство.

Сейчас интерфейс не различает эти режимы.

---

## 6. Нет safe-area логики

Safari / iPhone / iPad / мобильные браузеры имеют системные зоны:

- верхняя панель браузера;
- нижняя панель браузера;
- жестовые зоны;
- status bar;
- safe area.

Текущий UI не учитывает эти зоны.

---

# Карта изменений по файлам

## js/data.js

Не изменяется.

Причина:
Stage 02.4.4 не меняет баланс, башни, врагов, карту или экономику.

---

## js/state.js

Добавить состояние responsive UI:

- `uiLayout`
- `deviceMode`
- `orientation`
- `isMobile`
- `isLandscape`
- `isPortrait`

Назначение:

хранить состояние текущего интерфейсного режима, чтобы `ui.js` мог строить правильный layout.

---

## js/systems.js

Не изменяется.

Причина:
Stage 02.4.4 не меняет gameplay-логику.

---

## js/ui.js

Основной файл изменений.

Нужно переработать:

- HUD;
- bottom controls;
- zoom controls;
- speed controls;
- wave panel;
- Codex positioning;
- menu positioning;
- notification layer;
- mobile button sizes;
- portrait layout;
- landscape layout.

Главная задача:

уменьшить визуальный шум и освободить карту.

---

## js/game.js

Внести минимальные изменения:

- обновлять layout при resize;
- обновлять layout при смене ориентации;
- вызывать функцию определения device layout.

---

## index.html

Проверить и при необходимости уточнить:

- viewport;
- safe-area compatibility;
- mobile scaling;
- touch behavior.

---

## css/style.css

Возможно потребуется:

- вынести часть inline-стилей из `index.html`;
- добавить mobile-safe стили;
- добавить базовые CSS-переменные для safe-area;
- подготовить responsive CSS.

Если на текущем этапе достаточно JS-стилей в `ui.js`, `style.css` можно не трогать.

---

# Что нужно реализовать в Stage 02.4.4

## 1. Responsive layout detector

Добавить определение режима:

- mobile portrait;
- mobile landscape;
- tablet portrait;
- tablet landscape;
- desktop / wide screen.

---

## 2. Compact HUD

Переработать верхний HUD.

Цель:

сделать его компактным и не перекрывающим карту.

Возможный вариант:

~~~text
🌲78  🪨20  🍖10  ❤️100  ⚡0/10
~~~

А вторичные данные:

- сложность;
- zoom;
- номер волны;

показывать компактнее или в отдельном мини-блоке.

---

## 3. Убрать дублирование zoom

Сейчас zoom отображается и сверху, и в нижней панели.

Нужно оставить один понятный вариант.

---

## 4. Объединить zoom и speed controls

Сейчас это две отдельные группы.

Нужно сделать один компактный control cluster:

~~~text
-  +  100%
x1 x2 x3
~~~

Но разместить так, чтобы он не мешал основным кнопкам.

---

## 5. Переработать bottom buttons

На телефоне кнопки должны быть компактнее.

Возможные режимы:

## Portrait

Использовать короткие кнопки:

~~~text
🏹  ⚔️  ℹ  ☰
~~~

## Landscape

Можно оставить подписи, если хватает места:

~~~text
🏹 Башня  ⚔️ Волна  ℹ Info  ☰ Меню
~~~

---

## 6. Перенести wave panel в compact mode

Текущая большая панель должна стать компактнее.

Возможный формат:

~~~text
Волна 7 | Подготовка | 0/25 | x1
~~~

Или:

~~~text
🌊 7   0/25   x1
~~~

---

## 7. Notification layer

Уведомления должны:

- не перекрывать HUD;
- не перекрывать bottom controls;
- не закрывать Game Over panel;
- быть читаемыми на телефоне;
- исчезать автоматически.

---

## 8. Codex / Menu positioning

Codex и Menu должны:

- не вылезать за экран;
- адаптироваться под portrait / landscape;
- иметь ограниченную высоту;
- при необходимости быть scrollable;
- не закрывать критичные кнопки.

---

## 9. Game Over panel

Проверить:

- читается ли на телефоне;
- не перекрывается ли браузером;
- кнопки доступны пальцем;
- retry и новая игра не конфликтуют.

---

## 10. Safe area

Учесть:

- `env(safe-area-inset-top)`;
- `env(safe-area-inset-bottom)`;
- `env(safe-area-inset-left)`;
- `env(safe-area-inset-right)`.

---

# Что НЕ делаем в Stage 02.4.4

На этом этапе не добавляем:

- генераторы энергии;
- новые башни;
- новые враги;
- upgrade tree;
- salvage;
- обломки;
- харвестеры;
- ремонт;
- fog of war;
- расширение карты;
- сохранения;
- новые ресурсы;
- новую экономику.

---

# Backlog / Пул будущих задач

## Gameplay backlog

- генераторы энергии;
- увеличение лимита энергии;
- апгрейды башен;
- новые типы башен;
- новые типы врагов;
- воздушные враги;
- anti-air башни;
- emergency tools;
- развитие базы;
- баланс волн;
- выход из gameplay ceiling на 7 волне.

---

## Economy backlog

- дерево копится, но тратить его некуда;
- нужна инфраструктура;
- нужны склады;
- нужны переработчики;
- нужна экономика восстановления;
- нужна логика ремонта.

---

## Recovery backlog

- обломки после разрушения;
- salvage objects;
- биомасса после врагов;
- сбор ресурсов с карты;
- харвестеры;
- переработка;
- блокировка клетки обломками.

---

## World backlog

- fog of war;
- расширение карты;
- открытие новых зон;
- тёмные неразведанные области;
- захват территории;
- ресурсные точки.

---

## UX backlog

- onboarding перед первой игрой;
- стартовое меню выбора сложности;
- persistent build mode;
- возможность быстро ставить несколько башен подряд;
- contextual popup около клетки;
- улучшенный Codex;
- разделение Codex на вкладки;
- лучшее объяснение энергии;
- better right-hand controls.

---

## Technical backlog

- дальнейшая декомпозиция `ui.js`, если он станет слишком большим;
- вынос стилей из `index.html`;
- подготовка PWA;
- подготовка Android wrapper;
- сохранения;
- localStorage;
- тестирование на разных устройствах.

---

# Задание тестировщику после реализации Stage 02.4.4

## 1. Проверить телефон portrait

Проверить:

- HUD помещается;
- кнопки не закрывают карту;
- zoom / speed доступны;
- bottom controls не перекрывают важную область;
- wave status читается;
- уведомления не мешают;
- Game Over panel читается;
- Codex открывается нормально.

---

## 2. Проверить телефон landscape

Проверить:

- HUD не превращается в кашу;
- кнопки не перекрывают карту;
- speed / zoom controls не конфликтуют с bottom buttons;
- wave panel не закрывает gameplay;
- управление пальцем остаётся удобным.

---

## 3. Проверить iPad portrait

Проверить:

- интерфейс не слишком крупный;
- карта занимает основную часть экрана;
- HUD читаемый;
- Codex и Menu не закрывают всё поле.

---

## 4. Проверить iPad landscape

Проверить:

- верхний HUD не перегружен;
- кнопки не мешают gameplay;
- zoom и speed доступны;
- Game Over panel корректно отображается.

---

## 5. Проверить gameplay после UI-изменений

Проверить, что не сломались:

- строительство;
- подтверждение строительства;
- отмена строительства;
- выбор башни;
- продажа башни;
- запуск волны;
- атака башен;
- враги;
- HP базы;
- Retry Wave;
- Full Restart;
- Codex;
- Menu;
- zoom;
- drag камеры.

---

## 6. Зафиксировать ощущения

После теста записать:

- стало ли меньше визуального шума;
- стало ли удобнее играть на телефоне;
- стало ли удобнее играть на iPad;
- какой режим удобнее: portrait или landscape;
- что всё ещё мешает;
- какие элементы нужно убрать / уменьшить / перенести;
- что переносим в Stage 02.4.5 или Stage 02.5.

---

# Критерии успешности

Stage 02.4.4 считается успешным, если:

- игра запускается;
- все механики Stage 02.4.3 продолжают работать;
- UI стал заметно компактнее;
- телефон portrait стал пригоден для тестирования;
- телефон landscape стал пригоден для тестирования;
- iPad layout стал чище;
- HUD больше не перекрывает половину карты;
- notification layer не конфликтует с кнопками;
- zoom / speed / bottom controls не создают хаос.

---

# Общий вывод

Stage 02.4.4 — это этап адаптации интерфейса под реальные устройства.

После него проект должен стать не только технически рабочим, но и пригодным для регулярного тестирования на телефоне и планшете.

Только после этого имеет смысл переходить к Stage 02.5 и добавлять новые gameplay-системы.