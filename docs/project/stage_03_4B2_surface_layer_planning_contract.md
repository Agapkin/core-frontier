# Stage 0.3.4B2 — Surface Layer Planning & Repository Blueprint Stabilization

## 1. Stage Status

Status:
planned_audit_stage

Classification:
bounded_repository_cognition_stage

Primary focus:

* Repository Map section 04 surface layer planning;
* surface topology audit preparation;
* machine-readable structure preservation;
* split/no-split evaluation for 04.

This stage follows completion and merge of:

Stage 0.3.4B1 — relationship topology stabilization.

B2 does NOT reopen stabilized 03 topology architecture.

---

## 2. Background

Stage 0.3.4B1 stabilized:

* Repository Map relationship topology;
* split relationship package 03a–03g;
* bounded topology doctrine;
* source-routed evidence verification;
* anti-entropy relationship population workflow.

B2 prepares bounded planning and audit structure for Repository Map section 04.

---

## 3. Why B2 Exists

B2 exists to support:

* surface-domain visibility;
* surface boundary identification;
* split pressure evaluation;
* machine-readable Repository Map structure;
* bounded audit preparation for 04.

Current runtime/source structure may contain semantically mixed domains.

Surface split planning may diverge from current physical source structure where operationally justified.

B2 establishes planning and audit constraints before future 04 population work.

---

## 4. Scope

Allowed B2 activities:

* audit-first planning for Repository Map 04;
* surface topology analysis;
* surface-domain boundary identification;
* machine-readability stabilization;
* split/no-split evaluation for 04;
* evidence-routing preparation through 06;
* debt tracking;
* bounded doctrine alignment where operationally necessary.

Potential surface domains include:

* runtime shell;
* DOM roots;
* UI panels;
* controls;
* canvas world surfaces;
* canvas entity surfaces;
* overlays;
* notification surfaces;
* CSS/selectors where operationally relevant.

B2 may prepare:

* bounded surface schemas;
* split candidates;
* evidence routing requirements;
* topology constraints.

B2 does NOT require immediate 04 population.

---

## 5. Non-Goals

B2 does NOT:

* refactor runtime/source code;
* redesign gameplay systems;
* reopen 03 topology stabilization;
* create exhaustive UI inventories;
* generate parser dumps;
* replace runtime authority;
* create speculative architecture systems;
* create ontology engines;
* populate unrelated Repository Map layers.

B2 also does NOT:

* create gameplay catalogs;
* create automation pipelines;
* redesign Discovery;
* merge AI reasoning archives into Repository Map.

---

## 6. Surface Layer 04 Working Hypothesis

Repository Map section 04 is treated as:

surface architecture layer

rather than:

visual inventory layer only.

04 should support visibility of:

* runtime presentation boundaries;
* UI/runtime-shell boundaries;
* canvas/render boundaries;
* overlay ownership boundaries;
* interaction surfaces;
* operational UI domains.

04 split planning may diverge from current physical source structure where operationally justified.

Surface topology planning should prioritize:

* operational readability;
* semantic boundaries;
* split pressure visibility;
* surface ownership visibility.

04 planning must avoid monolithic aggregation structures.

---

## 7. Audit-First Workflow

B2 must follow audit-first workflow discipline.

Required workflow:

1. audit current runtime/surface structure;
2. identify candidate surface domains;
3. inspect semantic overlap pressure;
4. inspect possible split pressure;
5. verify source-confirmed surface ownership;
6. inspect evidence routing requirements;
7. evaluate split/no-split viability;
8. perform bounded approved population only if explicitly authorized.

When uncertain:

prefer omission over speculative topology.

---

## 8. Potential 04 Split Candidates

Possible future split candidates may include:

* runtime shell surfaces;
* DOM/control surfaces;
* panel/overlay surfaces;
* canvas world surfaces;
* canvas entity surfaces;
* notification surfaces;
* CSS/surface authority references.

These candidates are planning hypotheses only.

B2 must not assume final split structure before audit completion.

---

## 9. Machine-Readability Requirements

Machine-readability is a first-class requirement.

Repository Map records should remain:

* parser-readable;
* scanner-readable;
* agent-readable;
* bounded;
* schema-safe.

Requirements:

* stable ids;
* stable field naming;
* centralized refs;
* bounded vocab usage;
* parse-safe structures;
* minimized duplicated addressing.

Free prose must not replace structured fields.

New ids/types/status values must be:

* source-derived;
* vocab-aligned;
* operationally justified.

---

## 10. Evidence Routing Requirements

04 planning records must support source-routed verification through 06.

Evidence routing must preserve:

surface record
→ evidence_id
→ 06 router
→ source/runtime authority

Repository Map remains:

interpretive topology layer.

Runtime/source files remain:

execution authority.

Discovery remains:

archival provenance layer only.

---

## 11. Relationship to 01 / 02 / 03 / 05 / 06 / 07

01:
filesystem/file inventory layer.

02:
function inventory layer.

03:
stabilized relationship topology layer.

04:
surface architecture planning layer.

05:
risk/pressure layer with unresolved future stabilization.

06:
source evidence router and provenance routing layer.

07:
future hotspot aggregation layer.

B2 may identify cross-layer pressure,
but must not redesign unrelated layers.

---

## 12. Completion Criteria

B2 may close only when:

* 04 role is stabilized;
* 04 split/no-split decision is documented;
* approved surface population strategy is stabilized;
* 04 evidence routing through 06 is verified;
* discovered debts are recorded;
* final audit/closure report exists;
* machine-readability constraints remain preserved;
* no topology explosion occurred.

---

## 13. Forbidden Actions

Forbidden during B2 unless explicitly authorized by future stages:

* runtime/source refactor;
* gameplay redesign;
* speculative semantic expansion;
* ontology construction;
* generated dependency graph creation;
* broad parser extraction dumps;
* unbounded prose accumulation;
* duplicated routing values;
* uncontrolled vocab growth;
* replacing source authority with Repository Map;
* converting AI reasoning archives into canonical Repository Map truth.