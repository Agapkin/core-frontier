# Stage 02.4.5-A — Mobile Fix Pack / Input & Layout Stabilization

## Цель этапа

Stage 02.4.5 успешно разделил UI-слой на модули и убрал старый монолитный `js/ui.js`.

Однако тестирование на iPad и iPhone показало, что после архитектурной разрезки остались критические mobile UX и input-проблемы.

Цель Stage 02.4.5-A:

- стабилизировать строительство башен;
- исправить выбор клетки;
- исправить работу кнопки “Построить”;
- стабилизировать zoom;
- переработать mobile layout;
- улучшить landscape layout;
- уменьшить Codex на телефоне;
- привести controls к более удобной mobile-логике.

---

# Главные проблемы

## 1. Build Confirm Bug

Симптом:

- игрок выбирает клетку;
- клетка визуально подсвечивается;
- build overlay отображается;
- но при нажатии “Построить” появляется сообщение:
  “Сначала выбери клетку”;
- башня не строится.

Нужно проверить:

- `pendingBuildTile`;
- `selectBuildTile`;
- `confirmBuild`;
- canvas tap;
- DOM button click;
- pointer events;
- z-index;
- не сбрасывается ли выбранная клетка между тапом по canvas и тапом по кнопке.

---

## 2. Coordinate Mapping Instability

Симптом:

- клетка выбирается не совсем там, куда нажал игрок;
- особенно заметно на телефоне и планшете;
- при zoom ошибка ощущается сильнее.

Нужно проверить:

- `screenToWorld`;
- `screenToTile`;
- `worldToScreen`;
- `camera.zoom`;
- canvas bounding rect;
- Safari viewport;
- safe-area;
- mobile browser offsets.

---

## 3. Zoom Stabilization

Симптом:

- zoom работает;
- pinch zoom работает;
- кнопки +/- работают;
- но zoom можно “сбить” в неудобное состояние;
- нижняя кнопка “100%” не показывает текущий zoom;
- верхний zoom indicator обновляется, нижний — нет.

Нужно решить:

- либо убрать нижний процент;
- либо сделать динамическую кнопку;
- либо заменить кнопку “100%” на простую кнопку reset zoom.

---

## 4. Codex Mobile Layout

Симптом:

- Codex открывается;
- но на телефоне закрывает почти весь экран;
- мешает gameplay;
- выглядит как слишком крупная панель для mobile.

Нужно:

- уменьшить размер Codex;
- сделать компактный режим;
- ограничить высоту;
- возможно, сделать scroll внутри панели;
- отделить Codex от gameplay area.

---

## 5. Landscape Layout

Симптом:

- в горизонтальном режиме панели перекрывают карту;
- wave status занимает слишком много места;
- bottom controls мешают обзору;
- gameplay area уменьшается.

Нужно:

- сделать отдельный layout для landscape;
- уменьшить панели;
- перенести controls к краям;
- освободить центр карты.

---

## 6. Controls Placement

Симптом:

- кнопки “Башня”, “Волна”, “Codex”, “Меню” расположены неидеально для mobile;
- action-кнопки должны быть ближе к рабочему пальцу;
- информационные кнопки должны быть отделены от боевых действий.

Предварительное направление:

- “Башня” и “Волна” ближе к правому пальцу;
- speed controls рядом с action buttons;
- zoom controls отдельно;
- Codex и Menu компактно и отдельно;
- portrait и landscape могут иметь разные схемы.

---

# Блоки выполнения

## Блок A — Input + Build Confirm

Файлы:

- `js/game.js`
- `js/systems.js`

Задачи:

- проверить lifecycle `pendingBuildTile`;
- исправить `confirmBuild`;
- убедиться, что выбранная клетка не сбрасывается;
- проверить tap / pointer handling;
- стабилизировать выбор клетки;
- исправить смещение выбора клетки.

Критерий успеха:

- игрок выбирает клетку;
- клетка подсвечивается;
- кнопка “Построить” строит башню;
- ошибка “Сначала выбери клетку” появляется только когда клетка реально не выбрана.

---

## Блок B — Zoom Stabilization

Файлы:

- `js/game.js`
- `js/ui/controls.js`
- `js/ui/panels.js`

Задачи:

- стабилизировать minZoom / maxZoom;
- проверить reset zoom;
- убрать дублирование zoom label;
- сделать отображение zoom понятным;
- не давать игроку застрять в неудобном масштабе.

Критерий успеха:

- zoom работает кнопками;
- pinch zoom работает;
- reset возвращает в нормальное состояние;
- индикаторы zoom не противоречат друг другу.

---

## Блок C — Mobile Layout / Controls Placement

Файлы:

- `js/ui/layout.js`
- `js/ui/controls.js`
- `js/ui/panels.js`

Задачи:

- переразместить controls;
- отделить action controls от info controls;
- улучшить portrait layout;
- улучшить landscape layout;
- уменьшить перекрытие карты;
- сделать управление удобнее для телефона.

Критерий успеха:

- в portrait интерфейс не перекрывает ключевую часть карты;
- в landscape центр карты остаётся свободным;
- кнопки действия доступны удобно;
- Codex/Menu не мешают боевым controls.

---

## Блок D — Codex Compact

Файлы:

- `js/ui/panels.js`
- `js/ui/layout.js`

Задачи:

- уменьшить Codex на телефоне;
- ограничить высоту панели;
- сделать compact mode;
- сделать Codex читаемым, но не перекрывающим всю игру.

Критерий успеха:

- Codex открывается;
- на телефоне не закрывает весь экран;
- его можно использовать как справку, не ломая gameplay.

---

## Блок E — Cleanup

Файлы:

- `js/systems.js`
- `js/game.js`

Задачи:

- убрать дублирующую функцию `updateNotifications`, если она ещё есть вне `notifications.js`;
- обновить stage notification в `game.js`;
- проверить, что нет старых ссылок на `ui.js`;
- проверить порядок вызовов UI-функций.

Критерий успеха:

- нет дублирующихся функций;
- нет устаревших stage-текстов;
- все UI-вызовы идут в новые модули;
- игра запускается без ошибок.

---

# Что НЕ делаем в Stage 02.4.5-A

На этом этапе не добавляем:

- новые башни;
- новые враги;
- генераторы;
- salvage;
- обломки;
- технологии;
- fog of war;
- расширение карты;
- экономику;
- инфраструктуру.

Stage 02.4.5-A — это stabilization stage.

---

# Методика выполнения

Все изменения выполняются только через полный файл.

Запрещено:

- частично вставлять куски;
- просить найти строку;
- вручную менять отдельные участки;
- смешивать несколько файлов без порядка.

Правильный workflow:

1. GPT читает актуальный ZIP.
2. GPT определяет затрагиваемые файлы.
3. GPT перечисляет список файлов.
4. GPT выдаёт файлы по одному.
5. Каждый файл выдаётся целиком.
6. Пользователь полностью заменяет содержимое файла.
7. Для каждого файла даётся отдельный commit title и commit description.
8. После всех изменений пользователь загружает свежий ZIP.
9. GPT проверяет целостность.
10. Только после проверки начинается тестирование.

---

# Ожидаемый результат

После Stage 02.4.5-A проект должен получить:

- рабочее строительство башни на mobile;
- стабильный выбор клетки;
- нормальный coordinate mapping;
- стабильный zoom;
- понятный zoom reset;
- compact Codex;
- улучшенный portrait layout;
- улучшенный landscape layout;
- более удобное расположение controls;
- чистую связь между UI-модулями и gameplay logic.

---

# Переход после Stage 02.4.5-A

После успешного тестирования нужно будет обновить:

- `architecture.md`;
- `roadmap.md`;
- при необходимости `mechanics.md`.

Только после этого можно переходить к Stage 02.5 — Recovery Economy & Infrastructure.