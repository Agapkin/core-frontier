# Stage 03.4B — Repository Archetypes & Repository Cognition Preparation

## 1. Статус Stage 03.4B

Текущее состояние проекта:

* Stage 03.3B remains frozen/stabilized baseline;
* Stage 03.4 planning/discovery active;
* Stage 03.4 execution/runtime evolution NOT STARTED;
* repository cognition layer emerging;
* synchronization protocols operationally verified;
* repository_map.yml remains deferred/not implemented.

Stage 03.4B является:

* repository archetype extraction stage;
* repository cognition preparation stage;
* repository structure interpretation stage;
* operational repository mapping preparation stage.

Stage 03.4B НЕ является:

* repository_map.yml implementation stage;
* parser/scanner implementation stage;
* governance engine stage;
* automation rollout stage;
* runtime rewrite stage;
* ECS/framework stage;
* synchronization automation stage.

Главная operational задача Stage 03.4B:

подготовить extraction-first foundation
для будущего repository cognition layer,
не разрушая bounded evolution philosophy.

---

## 2. Почему repository cognition требует archetype extraction

После Stage 03.3B repository стал:

* topology-readable;
* extraction-aware;
* synchronization-aware;
* operationally navigable.

Но repository cognition всё ещё остаётся fragmented.

Информация о repository structure сейчас распределена через:

* runtime topology;
* file headers;
* YAML navigation;
* docs structure;
* workflow documents;
* frozen snapshots;
* synchronization protocols.

Следовательно:

repository пока ещё не имеет:

* unified repository archetype visibility;
* centralized operational mapping;
* explicit authority-zone visibility;
* stable interpretation-layer classification.

Без archetype extraction repository cognition рискует снова уйти в:

* implicit assumptions;
* stale synchronization;
* duplicated interpretation;
* hidden propagation pressure;
* coordination ambiguity.

---

## 3. Repository layers overview

Stage 03.4B formalizes repository layers.

### Runtime layer

Runtime layer включает:

* js/*
* runtime orchestration
* gameplay logic
* render/update lifecycle
* runtime state mutations
* runtime topology

Runtime layer является:

* runtime authority layer;
* execution layer;
* actual gameplay/runtime behavior source.

---

### Surface layer

Surface layer включает:

* index.html
* css/style.css
* runtime shell structure
* browser entrypoint visibility

Surface layer определяет:

* runtime shell;
* visual/runtime bootstrap;
* active/inactive surface resources.

---

### Interpretation layer

Interpretation layer включает:

* file headers;
* YAML navigation;
* semantic markers;
* docs;
* roadmap;
* snapshots;
* governance notes;
* synchronization protocols;
* future repository_map.yml.

Interpretation layer помогает:

* navigation;
* synchronization;
* impact awareness;
* repository cognition.

Но interpretation layer НЕ является runtime authority.

---

### Historical layer

Historical layer включает:

* stage documents;
* frozen snapshots;
* reports;
* operational findings;
* synchronization discoveries;
* governance evolution history.

Historical layer сохраняет:

* operational lineage;
* repository evolution context;
* bounded institutional memory.

---

### Governance layer

Governance layer включает:

* workflow.md
* synchronization protocols
* mutation discipline
* verification discipline
* bounded continuation discipline

Governance layer определяет:

* operational workflow;
* synchronization expectations;
* verification obligations.

Governance layer НЕ управляет runtime напрямую.

---

## 4. Runtime truth vs interpretation layers

Stage 03.4B окончательно закрепляет distinction между:

### Repository truth

Repository truth определяется:

* runtime code;
* actual topology;
* actual runtime behavior;
* actual runtime mutations;
* actual script/runtime order;
* actual repository structure;
* actual repository state.

Repository truth является authoritative source.

---

### Interpretation layers

Interpretation layers:

* описывают repository;
* помогают navigation;
* помогают synchronization;
* помогают cognition;
* помогают impact awareness.

Но interpretation layers:

* НЕ заменяют runtime truth;
* НЕ являются execution authority;
* НЕ должны становиться second repository reality.

---

## 5. Repository archetypes

Stage 03.4B вводит concept repository archetypes.

Archetype =
повторяющийся operational type repository entity.

---

### Runtime archetypes

#### Runtime orchestration files

Примеры:

* game.js
* systems.js

Role:

* sequencing;
* lifecycle orchestration;
* runtime coordination.

---

#### Lifecycle cluster files

Примеры:

* systems_wave_manager.js
* systems_placement.js
* systems_selected_object_actions.js

Role:

* cohesive runtime responsibilities;
* bounded mutation zones;
* extraction-oriented lifecycle ownership.

---

#### Runtime state files

Примеры:

* state.js
* data.js

Role:

* runtime state;
* runtime configuration;
* runtime topology dependencies.

---

### UI archetypes

Примеры:

* panels.js
* controls.js
* canvas_world.js
* canvas_entities.js
* notifications.js

Role:

* rendering;
* UI coordination;
* interaction visualization;
* DOM/runtime bridge.

---

### Interpretation archetypes

Примеры:

* ai/current_status.yml
* ai/docs_map.yml
* future repository_map.yml

Role:

* compressed repository interpretation;
* navigation;
* synchronization visibility.

---

### Governance archetypes

Примеры:

* workflow.md
* synchronization_protocols.md

Role:

* operational discipline;
* synchronization logic;
* verification protocols.

---

### Historical archetypes

Примеры:

* stage files
* frozen snapshots
* findings documents

Role:

* bounded historical continuity;
* operational evolution memory;
* stabilization evidence.

---

## 6. Authority-sensitive files

Stage 03.4B formalizes authority-sensitive files.

Authority-sensitive files =
files, изменение которых влияет на repository interpretation authority.

Примеры:

* workflow.md
* synchronization_protocols.md
* ai/current_status.yml
* ai/docs_map.yml
* future repository_map.yml
* runtime_structure.md

Изменения таких файлов могут требовать:

* synchronization review;
* interpretation verification;
* propagation awareness.

---

## 7. Synchronization-sensitive files

Synchronization-sensitive files =
files с повышенным synchronization pressure.

Причины:

* множество interpretation references;
* topology references;
* ownership references;
* workflow references;
* navigation references.

Примеры:

* game.js
* systems.js
* state.js
* workflow.md
* runtime_structure.md
* future repository_map.yml

Важно:

synchronization sensitivity
НЕ означает runtime criticality.

---

## 8. Runtime-critical files

Runtime-critical files =
files с высоким runtime impact.

Примеры:

* game.js
* state.js
* systems.js
* placement lifecycle
* render/update lifecycle

Runtime-critical files требуют:

* bounded verification;
* runtime safety discipline;
* topology-aware mutation planning.

Но могут иметь минимальный docs synchronization pressure.

---

## 9. Historical/frozen layers

Historical/frozen layers остаются intentionally preserved.

Примеры:

* Stage 03.3B frozen snapshot;
* previous stage files;
* findings artifacts;
* synchronization discovery snapshots.

Главная задача frozen layers:

* preservation of operational lineage;
* preservation of verified discoveries;
* prevention of rediscovery chaos.

Frozen layers НЕ должны:

* continuously mutate;
* become active governance engines;
* replace live repository truth.

---

## 10. Navigation anchor layers

Navigation anchors уже существуют внутри repository через:

* AI-readable headers;
* section boundaries;
* semantic markers;
* YAML navigation;
* synchronization protocols;
* architecture structure documents.

Stage 03.4B formalizes их как:

navigation anchor layer.

Navigation anchors помогают:

* repository scanning;
* topology visibility;
* extraction visibility;
* synchronization awareness;
* bounded cognition.

---

## 11. Почему repository_map.yml должен начинаться как skeletal layer

Stage 03.4B explicitly фиксирует:

repository_map.yml должен начинаться как:

* skeletal;
* bounded;
* extraction-first;
* interpretation-oriented;
* operationally minimal.

Repository map НЕ должен начинаться как:

* giant metadata graph;
* parser-generated monster;
* automation authority layer;
* governance engine;
* synchronization daemon.

Главный operational принцип:

repository_map.yml должен сначала
помогать человеку и AI понимать repository,
а НЕ пытаться автоматически управлять repository.

---

## 12. Почему extraction-first обязателен

Stage 03.4B фиксирует:

repository cognition должен строиться:

* из live repository truth;
* из verified topology;
* из stabilized headers;
* из existing operational structure.

А НЕ из:

* speculative architecture;
* imagined dependency systems;
* fake generic models;
* hallucinated runtime graphs.

Следовательно:

repository_map extraction должен идти:

* bounded passes;
* live verification;
* operational confirmation;
* incremental stabilization.

---

## 13. Runtime coupling vs synchronization coupling

Stage 03.4B сохраняет distinction между:

### Runtime coupling

Runtime coupling включает:

* runtime dependencies;
* lifecycle interaction;
* state mutation overlap;
* render/update sequencing;
* gameplay coordination.

Runtime coupling влияет на:

* runtime stability;
* extraction difficulty;
* verification complexity.

---

### Synchronization coupling

Synchronization coupling включает:

* YAML synchronization pressure;
* docs synchronization pressure;
* interpretation updates;
* navigation consistency;
* repository cognition maintenance.

Synchronization coupling влияет на:

* repository maintenance cost;
* drift risk;
* synchronization radius;
* propagation discipline.

Главный operational вывод:

runtime coupling
и
synchronization coupling
НЕ являются одним и тем же.

---

## 14. Safe local mutation vs architecture mutation

Stage 03.4B сохраняет operational distinction между:

### Safe local mutation

Safe local mutation:

* bounded runtime change;
* isolated verification;
* no topology drift;
* no ownership drift;
* minimal synchronization radius.

---

### Architecture mutation

Architecture mutation включает:

* topology changes;
* ownership changes;
* extraction-boundary changes;
* synchronization-layer impact;
* repository cognition impact.

Главный operational смысл:

это impact-classification layer,
а НЕ governance bureaucracy.

---

## 15. Synchronization protocols

Synchronization protocols remain passive.

Protocols:

* помогают synchronization;
* помогают bounded continuation;
* помогают propagation discipline.

Но protocols НЕ являются:

* governance authority engine;
* automation engine;
* runtime controller.

Новые protocols должны появляться только после:

* repeated operational patterns;
* verified real incidents;
* confirmed synchronization pain.

---

## 16. Overengineering risks

Stage 03.4B explicitly фиксирует основные risks.

### Governance explosion

Опасность:

repository cognition превращается в bureaucracy machine.

---

### Second repository reality

Опасность:

interpretation layers начинают конфликтовать с runtime truth.

---

### Speculative cognition

Опасность:

repository map начинает описывать imagined architecture,
а не actual repository state.

---

### Automation-before-understanding

Опасность:

automation внедряется до стабилизации repository cognition.

---

### Synchronization overload

Опасность:

каждый mutation требует full repository synchronization.

---

### Protocol proliferation

Опасность:

protocols появляются без реальной operational necessity.

---

## 17. Deferred/not-implemented areas

Следующие направления остаются deferred/not implemented:

* repository_map.yml implementation;
* parser/scanner implementation;
* governance engine;
* synchronization automation;
* runtime automation tooling;
* contracts system;
* ECS/entity framework;
* import/export migration;
* autonomous synchronization;
* auto-generated repository cognition;
* runtime authority engine.

---

## 18. Recommended next safe step

Следующий safest direction:

Stage 03.4A continuation
→ repository archetype extraction refinement
→ skeletal repository_map planning.

Без:

* automation rollout;
* parser implementation;
* governance expansion;
* runtime rewrite;
* synchronization engine creation.

---

## 19. Operational conclusions

Stage 03.4B operationally подтверждает:

repository evolution больше НЕ является только runtime problem.

Repository evolution теперь включает:

* repository cognition;
* synchronization discipline;
* interpretation consistency;
* impact awareness;
* bounded propagation;
* operational repository mapping.

Главный operational вывод:

repository cognition должен оставаться:

* bounded;
* extraction-first;
* runtime-truth-driven;
* verification-oriented;
* anti-overengineering.

Repository_map.yml должен стать:

compressed operational interpretation layer.

НЕ governance engine.
НЕ runtime authority.
НЕ second repository reality.

---

## 20. Текущий operational status

Текущий статус:

* Stage 03.3B remains frozen/stabilized baseline;
* Stage 03.4 planning/discovery active;
* Stage 03.4 execution/runtime evolution NOT STARTED;
* repository cognition layer emerging;
* synchronization protocols remain passive;
* repository_map.yml remains deferred/not implemented.

Главный operational принцип:

сначала repository должен научиться
понимать собственную структуру и влияние изменений.

Только потом —
автоматизировать что-либо поверх этого понимания.
