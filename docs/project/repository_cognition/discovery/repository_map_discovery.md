# Repository Map Discovery — Overview / Workflow / Batch Navigation

## 1. Назначение документа

Этот файл является lightweight root overview для repository cognition discovery chain.

Он НЕ хранит detailed extraction blocks напрямую.

Detailed source-faithful extraction content lives in bounded batch files:

- `docs/project/repository_cognition/discovery/repository_map_discovery_01_runtime_orchestration.md`
- `docs/project/repository_cognition/discovery/repository_map_discovery_02_extracted_systems.md`
- `docs/project/repository_cognition/discovery/repository_map_discovery_03_ui_render_control.md`
- `docs/project/repository_cognition/discovery/repository_map_discovery_03a_ui_controls.md`
- `docs/project/repository_cognition/discovery/repository_map_discovery_03b_canvas_world.md`
- `docs/project/repository_cognition/discovery/repository_map_discovery_03c_canvas_entities.md`
- `docs/project/repository_cognition/discovery/repository_map_discovery_04_foundation_state.md`
- `docs/project/repository_cognition/discovery/repository_map_discovery_05_wave_manager.md`
- `docs/project/repository_cognition/discovery/repository_map_discovery_06_ui_support.md`
- `docs/project/repository_cognition/discovery/repository_map_discovery_07_panels.md`
- `docs/project/repository_cognition/discovery/repository_map_discovery_08_surface_entry.md`

Root discovery file exists to preserve:

- discovery purpose;
- cognition-maintenance workflow;
- extraction/correction/re-check discipline;
- drift-detection philosophy;
- batch-chain navigation;
- coverage completion state;
- continuation status;
- operational constraints;
- deferred areas.

This file is intentionally:

- lightweight;
- retrieval-safe;
- continuation-friendly;
- index-oriented;
- evidence-oriented;
- non-authoritative over runtime truth.

---

## 2. Runtime truth supremacy

Repository truth remains:

- actual code;
- actual runtime behavior;
- actual file structure;
- actual script order;
- verified repository state.

AI-readable headers, comments, discovery files and future maps are interpretation layers.

They are useful for repository cognition, but they are not runtime authority.

If code and markup diverge:

- code wins;
- divergence must remain explicitly visible;
- discovery must not silently normalize the mismatch;
- source files must not be rewritten without a separate bounded correction pass.

---

## 3. Discovery-chain classification

The completed Batch 01–08 discovery chain now acts as:

- evidence layer;
- extraction history;
- verification layer;
- runtime cognition artifact;
- future repository_map input layer.

It is NOT:

- runtime authority;
- repository_map.yml;
- dependency graph;
- parser/scanner output;
- automation system;
- governance engine.

The discovery chain should now be treated as completed runtime/surface evidence base for future compression/navigation work.

---

## 4. Experimental cognition-maintenance workflow

The repository operationally discovered an experimental cognition-maintenance cycle:

```text
code
↕
AI-readable markup
↕
discovery extraction
↕
future repository cognition
```

Validated cycle:

1. source file read;
2. source-faithful markup extraction;
3. code/runtime verification;
4. drift or incompleteness detection;
5. correction candidate classification;
6. separate bounded correction pass if needed;
7. re-check / re-verification pass;
8. continuation state update.

This workflow remains discovery-local.

Do NOT move this workflow into `workflow.md` unless a later explicit workflow canonization pass is approved.

---

## 5. Drift-detection philosophy

Discovery should preserve:

- source snapshot;
- extracted markup;
- verification state;
- drift visibility;
- correction candidates;
- mismatch history;
- uncertainty markers.

Discovery must NOT silently normalize:

- incomplete headers;
- stale ownership;
- implicit runtime contracts;
- missing section semantics;
- future-generic pressure;
- mixed responsibility.

Not all findings require source correction.

Correction candidate classes:

1. honest-but-compact header;
2. incomplete ownership visibility;
3. stale markup;
4. implicit runtime contract;
5. actual contradiction.

Only real incompleteness/drift should become correction candidates.

---

## 6. Oversized / truncation-prone batch handling rule

Discovery batches are themselves synchronization-sensitive repository cognition entities.

If a discovery batch becomes:

- too large;
- truncation-prone;
- synchronization-sensitive;
- unsafe for bounded full-file mutation;

then:

- do NOT repeatedly restore it as one giant extraction file;
- convert it into mini-chain structure instead.

Mini-chain structure:

### Root batch file

Root batch file should contain only:

- index;
- navigation;
- status;
- continuation;
- integrity warnings.

### Sub-batch files

Sub-batch files should contain:

- detailed extraction;
- verification;
- cognition notes;
- correction lifecycle.

Operational lessons verified by Batch 03:

- bounded sub-batches reduce connector/write pressure;
- post-write integrity verification is mandatory for large cognition files;
- discovery files themselves are synchronization-sensitive entities;
- extraction integrity drift can occur without runtime drift;
- restoration must use fresh source reads instead of overwrite-from-memory reconstruction.

This is discovery-local mutation-safety guidance.

---

## 7. Call-site vs ownership verification rule

Discovery must preserve the distinction:

```text
function call does NOT imply ownership
```

Ownership should be verified through:

- declaration / definition location;
- explicit source-header ownership;
- actual code/runtime behavior;
- confirmed file responsibility.

Cross-file calls should be classified as one of:

- runtime coupling;
- orchestration dependency;
- callback bridge;
- external dependency.

They should NOT automatically become:

- missing ownership;
- source-header omission;
- correction candidate.

Verified examples:

- `controls.js` calls `createMenuPanel()` / `createGameOverPanel()`, but ownership remains in `panels.js`;
- `game.js` bridges into placement / selected object systems without owning them;
- `systems.js` calls extracted wave helpers without owning wave generation helpers.

---

## 8. Helper visibility significance rule

Discovery must preserve the distinction:

```text
helper existence does NOT automatically require top-header ownership visibility
```

But helper omission must NOT be silently ignored either.

If a helper/function exists in code but is absent from top ownership visibility, discovery must run helper significance verification before classifying it.

Possible helper significance outcomes:

### 1. Authority / lifecycle / contract-significant helper

Examples:

- `confirmBuild()`;
- `buildRoadTiles()`.

Possible result:

- ownership visibility correction candidate.

### 2. Local / section-scoped maintenance helper

Example:

- `updateResponsiveLayout()`.

Possible result:

- section extraction and cognition notes are sufficient;
- no top-header correction required.

### 3. Uncertain helper significance

Possible result:

- preserve explicit uncertainty;
- mark as `needs_review`;
- avoid premature source-header mutation.

This rule exists to prevent:

- automatic header inflation;
- automatic helper propagation into top ownership surfaces;
- fake architecture normalization;
- silent omission acceptance;
- skipped runtime-significant ownership drift.

---

## 9. Correction workflow discipline

Source-header / markup correction passes must:

- follow discovery verification;
- preserve source-truth history;
- avoid silent normalization;
- avoid fake architecture claims;
- avoid overstating ownership;
- preserve uncertainty where needed;
- remain bounded;
- mutate only AI-readable markup/header/comment surfaces when explicitly requested.

Correction passes must NOT:

- mutate runtime logic;
- rewrite gameplay behavior;
- refactor systems;
- create dependency graphs;
- implement repository_map.yml;
- create parser/scanner tooling;
- canonize the experimental workflow into workflow.md.

---

## 10. Batch-chain navigation

### Batch 01 — Runtime orchestration

File:

- `docs/project/repository_cognition/discovery/repository_map_discovery_01_runtime_orchestration.md`

Owned source files:

- `js/systems/systems.js`
- `js/game.js`

Status:

- complete;
- source-faithful extraction preserved;
- verification blocks present;
- `js/game.js` source-header correction applied and re-checked;
- `systems.js` lifecycle density preserved as needs-review/future refactor pressure.

---

### Batch 02 — Extracted systems

File:

- `docs/project/repository_cognition/discovery/repository_map_discovery_02_extracted_systems.md`

Owned source files:

- `js/systems/systems_placement.js`
- `js/systems/systems_selected_object_actions.js`

Status:

- complete;
- source-faithful extraction preserved;
- verification blocks present;
- `systems_placement.js` source-header correction for `confirmBuild()` applied and re-checked;
- no active source-header correction candidates remain.

---

### Batch 03 — UI render/control index

File:

- `docs/project/repository_cognition/discovery/repository_map_discovery_03_ui_render_control.md`

Role:

- lightweight index/status/navigation layer for Batch 03 mini-chain.

Status:

- complete;
- root file intentionally lightweight;
- detailed extraction moved into 03a/03b/03c mini-chain files after truncation-pressure discovery.

---

### Batch 03a — UI controls

File:

- `docs/project/repository_cognition/discovery/repository_map_discovery_03a_ui_controls.md`

Owned source file:

- `js/ui/controls.js`

Status:

- complete;
- controls/panel call-site vs ownership distinction verified;
- no active source-header correction candidates remain.

---

### Batch 03b — Canvas world rendering

File:

- `docs/project/repository_cognition/discovery/repository_map_discovery_03b_canvas_world.md`

Owned source file:

- `js/ui/canvas_world.js`

Status:

- complete;
- render/placement overlay boundary verified;
- no active source-header correction candidates remain.

---

### Batch 03c — Canvas entity rendering

File:

- `docs/project/repository_cognition/discovery/repository_map_discovery_03c_canvas_entities.md`

Owned source file:

- `js/ui/canvas_entities.js`

Status:

- complete;
- render-only entity visualization boundary verified;
- no active source-header correction candidates remain.

---

### Batch 04 — Foundation / runtime state

File:

- `docs/project/repository_cognition/discovery/repository_map_discovery_04_foundation_state.md`

Owned source files:

- `js/data.js`
- `js/state.js`

Status:

- complete;
- foundation data/config authority verified;
- foundation mutable runtime state authority verified;
- duplicate `updateResponsiveLayout()` interpretation synchronized with Batch 06 audit clarification;
- no active source-header correction candidates remain.

---

### Batch 05 — Wave manager helper layer

File:

- `docs/project/repository_cognition/discovery/repository_map_discovery_05_wave_manager.md`

Owned source file:

- `js/systems/systems_wave_manager.js`

Status:

- complete;
- helper/lifecycle boundary with `systems.js/startWave()` verified;
- extracted systems helper coverage completed.

---

### Batch 06 — UI support layer

File:

- `docs/project/repository_cognition/discovery/repository_map_discovery_06_ui_support.md`

Owned source files:

- `js/ui/helpers.js`
- `js/ui/layout.js`
- `js/ui/notifications.js`

Status:

- complete;
- helper/layout/notification support boundaries verified;
- duplicate `updateResponsiveLayout()` needs-review zone preserved;
- no immediate source-header correction candidates remain.

---

### Batch 07 — UI panels semantic layer

File:

- `docs/project/repository_cognition/discovery/repository_map_discovery_07_panels.md`

Owned source file:

- `js/ui/panels.js`

Status:

- complete;
- mixed UI semantic/orchestration layer verified;
- gameplay callback bridge without gameplay ownership preserved;
- panels complexity preserved as future refactor pressure.

---

### Batch 08 — Surface / runtime entry layer

File:

- `docs/project/repository_cognition/discovery/repository_map_discovery_08_surface_entry.md`

Owned source files:

- `index.html`
- `css/style.css`

Status:

- complete;
- runtime entrypoint verified;
- script/bootstrap order verified;
- active inline CSS authority verified;
- inactive `css/style.css` surface artifact preserved as future activation/migration candidate.

---

## 11. Coverage completion state

Runtime JS coverage:

```text
14 / 14 files covered = 100%
```

Runtime + surface coverage:

```text
16 / 16 files covered = 100%
```

Covered runtime/surface files:

- `index.html` — Batch 08;
- `css/style.css` — Batch 08;
- `js/data.js` — Batch 04;
- `js/state.js` — Batch 04;
- `js/game.js` — Batch 01;
- `js/systems/systems.js` — Batch 01;
- `js/systems/systems_wave_manager.js` — Batch 05;
- `js/systems/systems_placement.js` — Batch 02;
- `js/systems/systems_selected_object_actions.js` — Batch 02;
- `js/ui/helpers.js` — Batch 06;
- `js/ui/layout.js` — Batch 06;
- `js/ui/controls.js` — Batch 03a;
- `js/ui/panels.js` — Batch 07;
- `js/ui/canvas_world.js` — Batch 03b;
- `js/ui/canvas_entities.js` — Batch 03c;
- `js/ui/notifications.js` — Batch 06.

Discovery cycle status:

```text
runtime/surface discovery cycle operationally completed
```

---

## 12. Preserved needs_review / future refactor backlog

Current preserved backlog:

- duplicate `updateResponsiveLayout()` split-residue candidate;
- runtime authority ambiguity around post-layout-load responsive recalculation;
- inline CSS authority vs inactive `css/style.css`;
- selector drift risk between `.resource` inline CSS and `.cf-chip` external CSS;
- `systems.js` lifecycle density;
- `panels.js` mixed semantic/orchestration pressure;
- CSS activation/migration risk if `css/style.css` is linked without bounded review;
- discovery package relocation remains deferred because connector create/delete mechanics are not safe for active cognition infrastructure.

These are NOT current runtime mutation commands.

They are preserved cognition/future planning items.

---

## 13. Continuation state after discovery completion

Next logical phase is NOT:

- fresh discovery;
- runtime refactor;
- gameplay change;
- automation rollout;
- parser/scanner implementation;
- repository_map.yml auto-generation.

Next logical phase is:

- repository cognition compression;
- repository map creation planning;
- compressed operational navigation layer design;
- future synchronization/refactor planning;
- later bounded automation possibilities only after cognition compression stabilizes.

Recommended immediate next pass:

- create or refine a compressed repository cognition/map planning artifact based on Batch 01–08 findings.

---

## 14. Operational constraints

Do NOT:

- create `repository_map.yml` without a separate explicit pass;
- create parser/scanner systems;
- create automation systems;
- create dependency graph;
- mutate runtime logic;
- mutate gameplay behavior;
- relocate discovery files through current connector create/delete mechanics;
- canonize this experimental workflow into `workflow.md` without explicit approval;
- turn discovery into governance engine.

This chain remains:

- evidence layer;
- discovery-local;
- bounded;
- connector-safe;
- non-authoritative over runtime truth.

---

## 15. Deferred/not-yet-implemented areas

Still deferred / not implemented:

- `repository_map.yml`;
- parser/scanner implementation;
- automation rollout;
- synchronization engine;
- governance engine;
- dependency graph generation;
- runtime_map.yml;
- contracts.yml;
- generated repository cognition;
- discovery package relocation via true `git mv` / tree-level workflow;
- source/runtime refactors derived from needs_review backlog.

---

## 16. Operational conclusion

Repository cognition discovery has completed one full runtime/surface cycle.

The discovery chain now provides:

- source-faithful extraction evidence;
- runtime truth verification;
- correction/re-check history;
- ownership/coupling distinctions;
- active needs_review backlog;
- full runtime/surface coverage state.

Detailed source-faithful extraction belongs in bounded batch files.

The next phase should compress this evidence into a smaller operational repository cognition/navigation layer.

That future layer must remain:

- runtime-truth-driven;
- bounded;
- non-authoritative over code;
- anti-overengineering;
- useful for humans and AI before any automation.
