# Repository Map Functions Layer
# Stage 0.3.4B — final bounded semantic function inventory

meta:
  status: stabilized_after_final_scope_correction
  purpose: "Bounded semantic inventory of significant runtime functions and orchestration semantics."
  scope: "Semantic function inventory only; not parser dump, full call graph or dependency graph."

scope_audit:
  parser_dump_drift: not_detected
  relationship_graph_drift: not_detected
  dependency_graph_drift: not_detected
  surfaces_layer_duplication: not_detected
  hotspot_layer_duplication: not_detected
  future_split_candidate: true
  split_decision_status: deferred

anti_entropy_notes:
  - "Function records are semantic inventory, not full source code."
  - "Do not expand this layer into complete parser output."
  - "Do not convert pressure markers into implementation claims."
  - "Relationship graph remains outside this file."
  - "Runtime/source code remains absolute authority."
